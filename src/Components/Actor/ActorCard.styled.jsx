import styled from 'styled-components';

//Import Syled Components to extend styles
import { SearchCard } from '../Style';

export const StyledActorCard = styled(SearchCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;
